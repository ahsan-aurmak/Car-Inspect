
  # Car inspection app (Pak)

  This is a code bundle for Car inspection app (Pak). The original project is available at https://www.figma.com/design/ZKbByCPR6hm76n1cQkks6Q/Car-inspection-app--Pak-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  